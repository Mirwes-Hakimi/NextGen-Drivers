import React from "react";

export default function CitySelector({ cities, selectCity, onCityChange}){
    /// this component gets alist of cities and a callback function when a city is selected
    return (
        <div >
        
        </div>
    )
}